# Este repositorio es PUBLICO y GitHub Pages sirve su contenido.
# Todo lo que se commitee queda accesible en internet.
# Documentos internos y archivos fuente de trabajo no entran.

# Documentacion interna.
# El patron cubre cualquier version: atarlo a "v3" dejo fuera la v4.
Spec*.md
*Brand Book*.pdf

# Fuentes de logo sin procesar (los procesados si se versionan:
# logo-actio-verde.png, logo-actio-blanco.png, logo-cliente.png)
Logo-Actio-Blanco-Transparente.png
Logo-Actio-Negro-Transparente.png
Logo-Actio-Verde-Tranparente.gif
Logo-Actio-Green2*.png

# Datos y copias de prueba del tablero de Kinder. Nunca se publican:
# GitHub Pages sirve todo lo commiteado.
_pruebas/
dashboard-kinder-diseno.html

# Direcciones reales de los CSV del Sheet de VEA Kids. dashboard-kinder.html
# es publico; las direcciones van aqui, no ahi.
config-local.js

# La configuracion del portal del cliente (cPanel): lleva las direcciones
# reales de los CSV y la clave. En el servidor vive DOS niveles por encima de
# la carpeta publicada, fuera de lo que nginx entrega. Aqui se nombra para que
# una copia traida del servidor no se pueda commitear por descuido.
portal_config.php
auth.php
